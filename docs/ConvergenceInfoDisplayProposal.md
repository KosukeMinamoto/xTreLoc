# 収束情報表示機能の提案

STD、MCMC、TRDモードの収束情報をGUI上で簡易的に表示する機能の提案です。

## 1. 表示方式の提案

### 案1: ログパネル内に構造化セクションを追加（推奨）

現在のログパネルに、収束情報専用のセクションを追加します。

**メリット:**
- 既存のUIを大きく変更しない
- 実装が簡単
- ログと一緒に確認できる

**表示内容:**

#### STDモード
```
=== Convergence Information ===
Iteration: 3/10
Evaluations: 45
Current Residual: 0.0234 s
Parameter Changes:
  Longitude: 0.000123 deg
  Latitude: 0.000098 deg
  Depth: 0.012 km
Outliers Removed: 2
```

#### MCMCモード
```
=== Convergence Information ===
Progress: 450/1000 samples (45%)
Acceptance Rate: 32.5%
Current Sample:
  Longitude: 142.345678 deg
  Latitude: 39.123456 deg
  Depth: 15.234 km
  Log-Likelihood: -12.345
Statistics (after burn-in):
  Mean: (142.345, 39.123, 15.23)
  Std: (0.012, 0.008, 0.45)
```

#### TRDモード
```
=== Convergence Information ===
Cluster: 3/5
Stage: 2/2 (distKm=20, iterNum=10)
Iteration: 7/10
Triple Differences: 1234
Residual RMS: 0.0456 s
Parameter Updates:
  Events Updated: 45
  Median Shift Removed: Yes
```

### 案2: 別パネルでテーブル形式表示

ログパネルの下に、収束情報専用のテーブルパネルを追加します。

**レイアウト:**
```
┌─────────────────────────────────────┐
│ 📝 Execution Log                    │
│ [ログ表示エリア]                    │
└─────────────────────────────────────┘
┌─────────────────────────────────────┐
│ 📊 Convergence Information          │
│ ┌─────────┬─────────┬───────────┐  │
│ │ Metric  │ Current │ Target    │  │
│ ├─────────┼─────────┼───────────┤  │
│ │ Iter    │ 3       │ 10        │  │
│ │ Residual│ 0.0234  │ < 0.01    │  │
│ │ ...     │ ...     │ ...       │  │
│ └─────────┴─────────┴───────────┘  │
└─────────────────────────────────────┘
```

### 案3: サイドパネルでリアルタイム更新

右側に折りたたみ可能なサイドパネルを追加し、リアルタイムで収束情報を更新します。

**表示例:**
```
┌──────────┬──────────────────────────┐
│          │ 📊 Convergence Status    │
│   Main   │ ──────────────────────── │
│   Panel  │ Mode: STD                │
│          │ ┌─────────────────────┐ │
│          │ │ Iteration: 3/10      │ │
│          │ │ Residual: 0.0234 s   │ │
│          │ │ [Progress Bar]      │ │
│          │ └─────────────────────┘ │
│          │                          │
│          │ [詳細を表示]            │
└──────────┴──────────────────────────┘
```

## 2. 実装詳細

### 2.1 インターフェース設計

各ソルバーから収束情報を取得するためのインターフェースを定義：

```java
public interface ConvergenceInfoProvider {
    ConvergenceInfo getConvergenceInfo();
}

public class ConvergenceInfo {
    public enum Status {
        RUNNING, CONVERGED, FAILED, CANCELLED
    }
    
    private Status status;
    private Map<String, Object> metrics;
    private List<String> messages;
    
    // Getters...
}
```

### 2.2 STDモードの収束情報

```java
// HypoStationPairDiff.java に追加
public class STDConvergenceInfo extends ConvergenceInfo {
    private int currentIteration;
    private int maxIterations;
    private int evaluations;
    private double currentResidual;
    private double[] parameterChanges;
    private int outliersRemoved;
    private boolean converged;
}
```

### 2.3 MCMCモードの収束情報

```java
// HypoMCMC.java に追加
public class MCMCConvergenceInfo extends ConvergenceInfo {
    private int currentSample;
    private int totalSamples;
    private double acceptanceRate;
    private double[] currentLocation;
    private double currentLogLikelihood;
    private double[] sampleMean;
    private double[] sampleStd;
}
```

### 2.4 TRDモードの収束情報

```java
// HypoTripleDiff.java に追加
public class TRDConvergenceInfo extends ConvergenceInfo {
    private int currentCluster;
    private int totalClusters;
    private int currentStage;
    private int totalStages;
    private int currentIteration;
    private int iterationsPerStage;
    private int tripleDifferencesCount;
    private double residualRMS;
    private int eventsUpdated;
}
```

## 3. 推奨実装（案1の拡張版）

### 3.1 ログパネルに収束情報セクションを追加

現在のログパネルを2つのセクションに分割：

1. **収束情報セクション**（上部、固定高さ）
   - 現在の収束状態を簡潔に表示
   - プログレスバー
   - 主要メトリクス

2. **詳細ログセクション**（下部、スクロール可能）
   - 従来のログ表示

### 3.2 表示コンポーネント

```java
private JPanel createConvergencePanel() {
    JPanel panel = new JPanel(new BorderLayout());
    panel.setBorder(BorderFactory.createTitledBorder("📊 Convergence Status"));
    
    // メトリクス表示エリア
    metricsArea = new JTextArea(4, 50);
    metricsArea.setEditable(false);
    metricsArea.setFont(new Font(Font.MONOSPACED, Font.PLAIN, 11));
    metricsArea.setBackground(new Color(240, 248, 255));
    
    // プログレスバー
    progressBar = new JProgressBar(0, 100);
    progressBar.setStringPainted(true);
    
    panel.add(metricsArea, BorderLayout.CENTER);
    panel.add(progressBar, BorderLayout.SOUTH);
    
    return panel;
}
```

### 3.3 更新メカニズム

SwingWorkerの`process`メソッドで収束情報を更新：

```java
@Override
protected void process(List<String> chunks) {
    for (String message : chunks) {
        if (message.startsWith("CONVERGENCE:")) {
            updateConvergenceInfo(parseConvergenceInfo(message));
        } else {
            appendLog(message);
        }
    }
}

private void updateConvergenceInfo(ConvergenceInfo info) {
    SwingUtilities.invokeLater(() -> {
        String mode = (String) modeCombo.getSelectedItem();
        String displayText = formatConvergenceInfo(mode, info);
        metricsArea.setText(displayText);
        
        // プログレスバー更新
        if (info.getProgress() >= 0) {
            progressBar.setValue((int)(info.getProgress() * 100));
            progressBar.setString(String.format("%.1f%%", info.getProgress() * 100));
        }
    });
}
```

## 4. 表示フォーマット例

### STDモード
```
┌─────────────────────────────────────────┐
│ STD Mode - Convergence Status           │
├─────────────────────────────────────────┤
│ Iteration:     3 / 10                   │
│ Evaluations:   45                       │
│ Residual:      0.0234 s                 │
│ Status:        Running                  │
│                                         │
│ Parameter Changes:                      │
│   ΔLon: 0.000123 deg                    │
│   ΔLat: 0.000098 deg                    │
│   ΔDep: 0.012 km                        │
│                                         │
│ Outliers Removed: 2                     │
└─────────────────────────────────────────┘
[████████░░░░░░░░░░░░] 30%
```

### MCMCモード
```
┌─────────────────────────────────────────┐
│ MCMC Mode - Convergence Status          │
├─────────────────────────────────────────┤
│ Progress:      450 / 1000 (45%)         │
│ Acceptance:    32.5%                    │
│ Status:        Sampling                 │
│                                         │
│ Current Sample:                         │
│   Lon: 142.345678°                      │
│   Lat: 39.123456°                      │
│   Dep: 15.234 km                       │
│   Log-L: -12.345                       │
│                                         │
│ Statistics (after burn-in):             │
│   Mean: (142.345, 39.123, 15.23)       │
│   Std:  (0.012, 0.008, 0.45)           │
└─────────────────────────────────────────┘
[████████████████░░░░] 45%
```

### TRDモード
```
┌─────────────────────────────────────────┐
│ TRD Mode - Convergence Status           │
├─────────────────────────────────────────┤
│ Cluster:       3 / 5                    │
│ Stage:         2 / 2 (distKm=20)        │
│ Iteration:     7 / 10                   │
│ Status:        Running                  │
│                                         │
│ Triple Differences: 1234                │
│ Residual RMS:  0.0456 s                 │
│ Events Updated: 45                      │
│                                         │
│ Current Stage Progress:                 │
│   Stage 1: ✓ Completed                 │
│   Stage 2: ███████░░░ 70%              │
└─────────────────────────────────────────┘
[████████████████░░░░] 60%
```

## 5. 実装の優先順位

1. **Phase 1（最小実装）**: ログパネルに構造化された収束情報を追加
   - 各モードの主要メトリクスを表示
   - プログレスバー追加

2. **Phase 2（拡張）**: テーブル形式の詳細表示
   - メトリクスの履歴表示
   - グラフ表示（オプション）

3. **Phase 3（高度）**: リアルタイム可視化
   - 残差の時系列グラフ
   - パラメータの収束過程の可視化

## 7. 残差推移グラフの実装

### 7.1 ResidualPlotPanelクラス

JFreeChartを使用して残差の推移をリアルタイムで表示するパネルを実装しました。

**主な機能:**
- リアルタイムで残差データを追加・更新
- STD/MCMC/TRDモードに対応
- 自動スケーリング機能
- グラフの画像エクスポート
- データクリア機能

**表示内容:**

#### STDモード
- 反復ごとの残差の推移
- 収束の様子を視覚的に確認

#### MCMCモード
- サンプルごとの残差の推移
- ログ尤度の推移（2軸表示）
- サンプリングの安定性を確認

#### TRDモード
- クラスターごと、反復ごとの残差の推移
- 各クラスターの収束状況を比較

### 7.2 GUIへの統合

HypocenterLocationPanelにResidualPlotPanelを追加：

```java
// レイアウト例
┌─────────────────────────────────────┐
│ 📝 Execution Log                    │
│ [ログ表示エリア]                    │
└─────────────────────────────────────┘
┌─────────────────────────────────────┐
│ 📊 Convergence Status                │
│ [メトリクス表示]                    │
└─────────────────────────────────────┘
┌─────────────────────────────────────┐
│ 📈 Residual Convergence Plot         │
│ [グラフ表示エリア]                  │
│ [Clear] [Export Image] [Auto Scale] │
└─────────────────────────────────────┘
```

### 7.3 ソルバーからのデータ取得

各ソルバーにコールバックを追加して、残差データをリアルタイムで取得：

```java
// インターフェース定義
public interface ConvergenceCallback {
    void onResidualUpdate(int iteration, double residual);
    void onLikelihoodUpdate(int sample, double logLikelihood); // MCMC用
    void onClusterUpdate(int clusterId, int iteration, double residual); // TRD用
}

// 使用例（STDモード）
solver.setConvergenceCallback((iter, res) -> {
    residualPlotPanel.addResidualPoint(iter, res);
});
```

## 6. 技術的な考慮事項

### 6.1 スレッドセーフティ
- SwingWorkerを使用してUIスレッドで更新
- 収束情報の更新は`SwingUtilities.invokeLater()`で実行

### 6.2 パフォーマンス
- 更新頻度を制限（例: 100msごと）
- 大量のデータはサンプリングして表示

### 6.3 ユーザビリティ
- 折りたたみ可能なセクション
- 表示/非表示の切り替え
- 色分けによる状態表示（緑: 正常、黄: 警告、赤: エラー）

